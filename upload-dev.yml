server:
  port: 8082

spring:
  application:
    name: leyou-upload

  servlet:
    multipart:
      max-file-size: 500MB
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
  instance:
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5


crosUrl:
    http://manager.leyou.com:81